@model IdeaWeb.Models.Category

@{
    ViewData["Title"] = "Edit";
      var role = Context.Session.GetString("_Role") != null ? Context.Session.GetString("_Role") : null;
  string message = "You do not have permission to access this page";
    if (role == "Staff"){
     <script>
        alert("@message");
        window.location.href = "https://localhost:7188/User/login";
    </script>
  }
}

<h1>Edit Category</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <span class="text-danger">@ViewBag.ErrorMessage</span><br />
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="checkbox" id="CancleDeleteCheckbox" name="CancleDeleteCheckbox" value="true" />   
                <label class="control-label"><h2>Cancle Delete</h2></label>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div>
            @* <div class="form-group">
                <input type="checkbox" id="DeleteCheckbox" name="DeleteCheckbox" value="true" />   
                <label class="control-label"><h2>Delete</h2></label>
                <span asp-validation-for="Status" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
