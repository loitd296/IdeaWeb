@model IEnumerable<IdeaWeb.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dob)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email)
            </th>
            <th>
                Email confirmation
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dob)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
            <td>
                @{
                   if(item.flag == 1){
                     <span class="button small green --jb-modal">confirmed</span>
                   }
                   else{
                     <span class="button small red --jb-modal">Unconfirmed</span>
                   }
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Name)
            </td>
            <td>
<<<<<<< Updated upstream
                @Html.DisplayFor(modelItem => item.Department.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
=======
                <a asp-action="Edit" asp-route-id="@item.id" class="button blue --jb-modal"><i class="mdi mdi-eye"></i></a> |
                <a asp-action="Details" asp-route-id="@item.id" class="button blue --jb-modal"><i class="mdi mdi-square-edit-outline"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.id" class="button red --jb-modal"><i class="mdi mdi-trash-can"></i></a>
>>>>>>> Stashed changes
            </td>
        </tr>
}
    </tbody>
</table>
