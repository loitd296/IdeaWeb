<div>
  <title>Idea Record</title>
  <div class="card">
    <div class="card-header">
      <h5 class="card-title mb-0 text-center w-1.0">Idea Record</h5>
    </div>
    <div style="margin-left: 20px">
      <a asp-controller="storeowner" asp-action="ExportExcel"
        ><i class="fa-solid fa-file-excel" style="font-size: 30px"></i
      ></a>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>
    <div>
      <canvas id="Idea Record2" class="w-1.0 h-1.0"></canvas>
    </div>
    <script>
      const labels2 = [@Html.Raw(ViewData["labels"])];
      const data2 = {
        labels: labels2,
        datasets: [{
          label: 'Idea',
          backgroundColor: [@Html.Raw(ViewData["Number_Color"])],
          borderColor: 'rgb(255, 99, 132)',
          data: [@ViewData["count"]],
          barPercentage: 0.5,
          barThickness: 80,
        }]
      };
      const config2 = {
        type: 'bar',
        data: data2,
        options: {
          scales: {
            yAxes: [{
              ticks: {
                beginAtZero: true
              }
            }]
          }
        }
      };
    </script>
    <script>
      const myChart2 = new Chart(
        document.getElementById("Idea Record2"),
        config2
      );
    </script>
  </div>
</div>
